import {RestModel} from "../../../common/rest-model";

export class Supplier extends RestModel {
    name: string;
    phone: string;
    email: string;
    description: number;
    address: string;
}

